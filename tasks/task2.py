# tasks/task2.py



def solve():
# # Ниже пишите решение задачии(Обязательно поставьте четыре пробела после функции Solve())
    try:
    # Запрашиваем у пользователя основание (a) и высоту (h)
    a_input = input("Введите длину основания (a): ")
    h_input = input("Введите длину высоты (h): ")

    # Преобразуем введенные строки в числа с плавающей запятой
    a = float(a_input)
    h = float(h_input)

    # Рассчитываем площадь треугольника по формуле S = 1/2 * a * h
    S = 0.5 * a * h

    # Выводим результат
    print(f"Площадь треугольника (S): {S}")
    except ValueError:
    print("Ошибка: Пожалуйста, вводите корректные числа.")

 


# Код ниже не трогать! он нужен для тестов
if __name__ == "__main__":
    solve()