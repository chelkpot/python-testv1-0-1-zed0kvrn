# tasks/task1.py



def solve():
# # Ниже пишите решение задачии(Обязательно поставьте четыре пробела после функции Solve())
    try:
    # Запрашиваем температуру в градусах Цельсия
    celsius = float(input("Введите температуру в градусах Цельсия: "))

    # Переводим в Фаренгейты по формуле: F = (9/5) * C + 32
    fahrenheit = (9 / 5) * celsius + 32

    # Выводим результат
    print(f"Температура в Фаренгейтах: {fahrenheit}")

    except ValueError:
    print("Ошибка: Пожалуйста, введите числовое значение.")

   

# Код ниже не трогать! он нужен для тестов
if __name__ == "__main__":
    solve()