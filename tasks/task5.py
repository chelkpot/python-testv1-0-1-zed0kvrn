# tasks/task1.py



def solve():
# Ниже пишите решение задачии(Обязательно поставьте четыре пробела после функции Solve())
    try:
    # Запрашиваем числа у пользователя и разделяем их по пробелам
    # split() разбивает строку на список, а map(int, ...) превращает каждую часть в число
    numbers = list(map(int, input("Введите числа через пробел: ").split()))

    # Создаем список квадратов этих чисел
    squares = [n ** 2 for n in numbers]

    # Превращаем список квадратов обратно в строку через пробел для красивого вывода
    result = " ".join(map(str, squares))

    print(f"Результат: {result}")

    except ValueError:
    print("Ошибка: Пожалуйста, вводите только целые числа через пробел.")


# Код ниже не трогать! он нужен для тестов
if __name__ == "__main__":
    solve()